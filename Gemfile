source 'https://rubygems.org'
gem 'jekyll', '~> 4.3.2'
gem 'jekyll-feed', '~> 0.17.0'
gem 'jekyll-seo-tag', '~> 2.8.0'
gem 'jekyll-sitemap', '~> 1.4.0'
gem 'webrick', '~> 1.8'

# If you want to use GitHub Pages, remove the "gem "jekyll"" above and
# uncomment the line below. To upgrade, run `bundle update github-pages`.
# gem "github-pages", group: :jekyll_plugins

# Jekyll 4 requires `webrick` to be explicitly listed as a dependency.
# If you are using Jekyll 3, you can remove this line.

# Windows does not include zoneinfo by default, so bundle exec jekyll serve would error
# uncomment the following line if you're running Windows
# gem 'tzinfo-data'

# If you have any plugins, put them here!
group :jekyll_plugins do
  # gem "jekyll-relative-links"
end

# For Jekyll 4, the default theme is no longer included. If you want to use
# a theme, uncomment the line below and replace "minima" with your theme name.
# gem "minima", "~> 2.5"